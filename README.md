# Monobank Finance Parser

A console application for analyzing Monobank transactions.

## How to run

1. Install .NET 8 and PostgreSQL
2. Configure `appsettings.json`
3. Run `dotnet ef database update`
4. Run `dotnet run`

## Technologies

- .NET 8, EF Core, PostgreSQL, Monobank API
